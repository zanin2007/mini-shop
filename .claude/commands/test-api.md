$ARGUMENTS API 엔드포인트에 대한 테스트 시나리오를 작성해줘.

다음을 포함해줘:
1. **정상 케이스**: 올바른 입력으로 기대 결과 확인
2. **인증/권한**: 토큰 없음, 만료 토큰, 다른 유저 접근
3. **입력 검증**: 빈 값, 음수, 초과값, 특수문자, SQL injection 시도
4. **동시성**: 같은 요청 동시 전송 시 결과
5. **경계값**: 재고 0, 포인트 0, 쿠폰 만료 직전

각 시나리오를 curl 명령어로 작성해줘.
서버가 localhost:5000에서 실행 중이라고 가정해.
