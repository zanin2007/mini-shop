프로젝트 전체를 분석해서 다음에 할 작업을 우선순위별로 추천해줘.

다음 관점으로 스캔해줘:

1. **보안 취약점**: Rate limiting 누락, 입력 검증 부족, 권한 우회 가능성
2. **성능 이슈**: N+1 쿼리, 누락된 인덱스, 불필요한 리렌더링
3. **동시성 버그**: FOR UPDATE 누락, 트랜잭션 없는 다중 테이블 수정
4. **코드 품질**: 중복 코드, 미사용 코드, 타입 안전성
5. **누락된 기능**: 기존 기능의 빠진 엣지 케이스, UX 개선점

각 항목을 심각도(Critical/High/Medium/Low)로 분류하고, 표로 정리해줘.
CLAUDE.md의 Known Issues도 현재 코드와 비교해서 상태(해결됨/미해결)를 알려줘.
