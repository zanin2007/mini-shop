server/config/initDB.js의 테이블 스키마와 server/controllers/ 내 모든 컨트롤러의 SQL 쿼리를 비교 분석해줘.

다음을 확인해줘:
1. **컬럼 불일치**: 쿼리에서 참조하는 컬럼이 스키마에 존재하는지
2. **누락된 인덱스**: WHERE, JOIN, ORDER BY에 사용되는 컬럼에 인덱스가 있는지
3. **FK 정합성**: JOIN 관계가 실제 테이블 구조와 맞는지
4. **타입 불일치**: 쿼리 파라미터 타입이 컬럼 타입과 호환되는지

표 형태로 정리해줘.
